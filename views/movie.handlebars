{{!-- <ul>
  {{#each movies}}
      {{> movies/movie-block }}
  {{/each}}
</ul>
//This was taken from index.handlebars
// when you render index, it expects an array
of movie objects and then renders a partial for each --}}